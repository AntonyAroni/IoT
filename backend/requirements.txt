# Dependencias del servidor IPS.
# Las herramientas de calibración y auditoría tienen las suyas en
# calibration_tools/requirements.txt (numpy vivía aquí pero el backend no lo usa).
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pydantic>=2.6.0
